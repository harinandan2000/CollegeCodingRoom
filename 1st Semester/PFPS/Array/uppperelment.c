//wap to find the sum of upper digonal element & lower diogonal element of matrix
//Harinandan paswan
//roll no- 22403
#include <stdio.h>
int main()
{
    int i, j, c, r, l_sum = 0, u_sum = 0;
    int A[5][5];
    printf("Enter the order of elements\n");
    scanf("%d%d", &r, &c);
    if(r == c)
    {
        printf("Inter the %d element in matrix",r*c);
        for(i=1; i<=r; i++)
        {
            for(j=1; j<=c; j++)
            {
                if(i>j)

            }
        }
    }
    
    return 0;
}